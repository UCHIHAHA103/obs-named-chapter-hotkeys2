# 构建错误记录

## 2026-03-22 构建失败：C2079 undefined class 'QJsonArray'

### 错误信息
```
chapter-hotkeys.hpp(37,13): error C2079: 'MarkerProfile::markers' uses undefined class 'QJsonArray'
```

### 原因分析
头文件 `chapter-hotkeys.hpp` 中 `MarkerProfile` 结构体使用了 `QJsonArray` 类型作为成员变量：
```cpp
struct MarkerProfile {
    QString name;
    QString description;
    QJsonArray markers;    // ← 需要完整类型定义
    bool enableComments;
};
```

但 `#include <QJsonArray>` 只在 `.cpp` 文件中包含，头文件中缺失该 include。
编译器在处理头文件时无法确定 `QJsonArray` 的大小，导致 `error C2079`。

同时 `MarkerLivePanel` 类使用了 `QTimer*` 成员，也需要 `<QTimer>` 头文件。

### 修复方案
在 `chapter-hotkeys.hpp` 中添加缺失的头文件：
```cpp
#include <QJsonArray>
#include <QTimer>
```

### 提交记录
- Commit `30aeccb`: fix: add missing QJsonArray and QTimer includes in header file

---

## 2026-03-21 崩溃：对话框重入导致 ACCESS_VIOLATION

### 崩溃堆栈
```
异常代码: c0000005 (ACCESS_VIOLATION)
崩溃位置: ChapterWithCommentDialog::AskForNameAndComment+0x80
调用链:   HotkeyPressed → QTimer::singleShot → ShowCommentDialog → AskForNameAndComment
```

### 原因分析
热键在独立线程中触发，通过 `QTimer::singleShot(0, ...)` 投递到 UI 线程。
当用户快速连按热键时，多个 `ShowCommentDialog` 请求排队执行，
在对话框构造/销毁期间再次进入，访问已释放的 Qt 对象。

旧版仅用 `static bool s_isDialogOpen` 检查，存在线程竞态条件。

### 修复方案
1. 将 `s_isDialogOpen` 改为 `std::atomic<bool>` 原子变量
2. 新增 `QMutex s_dialogMutex` 互斥锁保护对话框创建
3. 新增 `g_showDialogPending` 原子标志防止热键重复触发
4. 使用 `QMetaObject::invokeMethod` 替代 `QTimer::singleShot` 安全地在 UI 线程执行
5. `AskForNameAndComment` 中实现双重检查锁定（Double-Checked Locking）

### 提交记录
- Commit `40e53b2`: feat: add config profiles, import/export, live preview panel & fix dialog reentry
