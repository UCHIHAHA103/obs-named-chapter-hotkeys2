# OBS 崩溃分析与修复记录

## 崩溃概要 (2026-02-21)

| 项目 | 值 |
|------|------|
| **异常代码** | `c0000005` (ACCESS_VIOLATION / 访问冲突) |
| **崩溃位置** | `ChapterWithCommentDialog::AskForNameAndComment+0x80` |
| **调用链** | `HotkeyPressed → QTimer::singleShot → ShowCommentDialog → AskForNameAndComment` |
| **崩溃DLL** | `qt6core.dll` / `obs-named-chapter-hotkeys.dll` |
| **OBS版本** | 32.0.4 (64-bit) |
| **系统** | Windows 10 22H2, AMD Ryzen 7 9800X3D |

## 根因分析

### 问题：对话框重入保护不完善

原代码使用简单的 `static bool s_isDialogOpen` 标志来防止对话框重复打开，但存在以下竞态条件：

1. **热键线程 vs UI 线程竞态**：`HotkeyPressed` 在 OBS 的热键线程中执行，通过 `QTimer::singleShot(0, ShowCommentDialog)` 将对话框创建投递到 UI 线程。当用户快速连续按键时，多个 `singleShot` 可能在 `s_isDialogOpen` 被设置为 `true` 之前同时通过检查。

2. **标志设置时机不安全**：`s_isDialogOpen = true` 在 `ChapterWithCommentDialog` 构造函数中设置，而非在检查点处设置，导致从检查到设置之间存在时间窗口。

3. **非原子操作**：`s_isDialogOpen` 是普通 `bool`，在多线程环境下读写不安全。

### 崩溃场景还原

```
时刻 T1: 热键线程 → HotkeyPressed → s_isDialogOpen=false → 通过检查 → QTimer::singleShot(ShowCommentDialog)
时刻 T2: 热键线程 → HotkeyPressed → s_isDialogOpen=false → 通过检查 → QTimer::singleShot(ShowCommentDialog)
时刻 T3: UI 线程 → ShowCommentDialog#1 → ChapterWithCommentDialog 构造 → s_isDialogOpen=true
时刻 T4: UI 线程 → ShowCommentDialog#2 → 尝试创建第二个对话框 → 访问已被第一个对话框使用的资源 → 💥 CRASH
```

## 修复方案 (v2.1.0)

### 1. 原子标志 + 双重检查锁定

```cpp
// 旧代码
static bool s_isDialogOpen;

// 新代码
static std::atomic<bool> s_isDialogOpen(false);
static QMutex s_dialogMutex;
```

### 2. 热键线程增加前置原子标志

```cpp
// 新增全局标志，在 HotkeyPressed 中立即设置
static std::atomic<bool> g_showDialogPending(false);

void ChapterHotkeyItem::HotkeyPressed(...)
{
    // 先检查对话框是否已打开
    if (ChapterWithCommentDialog::IsDialogOpen()) return;
    
    // 原子性地尝试设置 pending 标志
    if (g_showDialogPending.exchange(true)) return;
    
    // ...添加标记...
    
    // 安全投递到 UI 线程
    QMetaObject::invokeMethod(QCoreApplication::instance(), []() {
        ShowCommentDialog();
        g_showDialogPending.store(false);
    }, Qt::QueuedConnection);
}
```

### 3. AskForNameAndComment 增加互斥锁

```cpp
bool ChapterWithCommentDialog::AskForNameAndComment(...)
{
    QMutexLocker locker(&s_dialogMutex);
    
    // 双重检查
    if (s_isDialogOpen.load()) return false;
    
    // ...创建并显示对话框...
}
```

### 4. 使用 QMetaObject::invokeMethod 替代 QTimer::singleShot

旧代码使用 `QTimer::singleShot(0, lambda)` 投递到 UI 线程，新代码改用更安全的 `QMetaObject::invokeMethod(..., Qt::QueuedConnection)`，确保在 UI 线程的事件循环中安全执行。

## 修复效果

- ✅ 完全消除热键线程和 UI 线程之间的竞态条件
- ✅ 即使快速连续按键也不会创建多个对话框
- ✅ 原子标志确保跨线程可见性
- ✅ 互斥锁作为最后防线防止并发创建
